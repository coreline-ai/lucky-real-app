# implement_20260713_154958.md

작성 일시: `2026-07-13 15:49:58 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적
프로젝트 종료 전 문서와 실제 구현·검증·Git 상태의 정합성을 맞추고 재현 가능한 종료 기준을 고정한다.

## 개발 범위
루트 README, engine/mcp-server/web-lucky/web-mcp-daily README, 이전 완료 계획서의 검증 명령과 문서 연결을 현재 구현 상태에 맞게 정리한다.

## 제외 범위
- 신규 기능 및 제품 아이디어 추가
- 엔진·MCP·웹앱 동작 변경
- PlayMCP 실제 등록 및 운영 HTTPS 배포
- 사용자 요청 없는 Git commit/push

## 참조 문서
- [이전 개발 계획](implement_20260713_103828.md)
- [루트 README](../README.md)
- [엔진 README](../engine/README.md)
- [MCP 서버 README](../mcp-server/README.md)
- [web-lucky README](../web-lucky/README.md)
- [web-mcp-daily README](../web-mcp-daily/README.md)

## 공통 진행 규칙
- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 요구사항이 불명확하면 가능한 케이스를 나누고 확인 후 진행한다.
- 한 기능은 유지보수 가능한 최소 책임 단위로 나누고, 각 단위의 역할과 경계를 계획에 적는다.
- 계획 밖 옵션/확장/부가 기능을 만들기 위한 분리는 하지 않는다.
- 공통화는 계획 범위 안의 중복을 줄일 때만 수행하고 과도한 추상화는 피한다.
- 기존 프로젝트 API, 공식 SDK, 표준 라이브러리를 우선 검토하고 새 의존성은 범위상 필요할 때만 포함한다.

## Phase 상태 요약
- [x] Phase 1. 종료 기준과 변경 범위 고정 완료
- [x] Phase 2. 문서 불일치 수정 완료
- [x] Phase 3. 문서 및 실행 명령 재검증 완료
- [x] Phase 4. 종료 상태 기록 완료

## QA 관점
- [x] 문서가 로컬 완료와 원격 반영 완료를 혼동하지 않는지 확인한다.
- [x] README의 실행 명령을 명시된 작업 디렉터리에서 그대로 실행한다.
- [x] `web-mcp-daily`가 엔진 직접 import나 local fallback을 허용한다고 오해할 표현이 없는지 확인한다.
- [x] PlayMCP 콘솔 등록과 운영 HTTPS 배포가 미완료 외부 과제로 남는지 확인한다.
- [x] 문서 수정으로 소스·빌드 산출물·의존성이 의도치 않게 변경되지 않았는지 확인한다.

---

## Phase 1. 종료 기준과 변경 범위 고정

### 목표
- 종료 시점에 고칠 문서 불일치와 고치지 않을 제품 범위를 명시한다.

### 구현 태스크
- [x] 최신 계획서의 완료 주장과 실제 로컬 구현을 대조한다.
- [x] 원격 `main`과 로컬 작업트리의 차이를 확인한다.
- [x] 문서 불일치를 README 상태 설명, 실행 명령, 문서 연결, 변동 수량으로 분류한다.
- [x] commit/push와 외부 PlayMCP 작업을 이번 범위에서 제외한다.

### 자체 테스트
- [x] `git status --short --branch`
- [x] `git rev-list --left-right --count HEAD...@{upstream}`
- [x] 핵심 README와 `implement_20260713_103828.md` 라인 단위 대조

### 이슈 및 수정
- [x] 기존 구현 묶음이 미커밋 상태임을 확인했으며 이번 문서 작업에서 임의로 원복하거나 push하지 않는다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 2. 문서 불일치 수정

### 목표
- 실제 구현과 검증 결과를 기준으로 종료 문서를 일관되게 수정한다.

### 구현 태스크
- [x] 루트 README의 두 웹앱 상태를 완료 구현과 남은 외부 과제로 분리한다.
- [x] 루트 모노레포 표에 Streamable HTTP와 `web-mcp-daily`를 반영한다.
- [x] 변동하기 쉬운 Dart 소스·엔진 테스트 파일 개수 표기를 제거한다.
- [x] 엔진 shard 생성 명령의 실행 위치를 명시한다.
- [x] MCP production smoke에 서버 실행 절차를 포함한다.
- [x] 이전 완료 계획서의 오래된 raw `rg` 검증을 MCP-only guard로 교체한다.
- [x] 루트와 각 하위 README에서 최신 구현 계획과 본 종료 계획을 연결한다.

### 자체 테스트
- [x] 수정 문서의 상대 링크가 모두 존재한다.
- [x] 문서에 `web-lucky` sharding이 미완료 과제로 남아 있지 않다.
- [x] `mcp-server`가 stdio 전용으로 설명되지 않는다.
- [x] 이전 계획서의 자체 테스트가 guard 명령과 일치한다.

### 이슈 및 수정
- [x] shard 생성 명령의 출력 경로가 npm `--prefix` 작업 디렉터리 기준으로 해석되는 문제를 발견해 `../web-lucky/...`로 수정했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 3. 문서 및 실행 명령 재검증

### 목표
- 문서에 기록한 명령과 핵심 완료 증거를 현재 작업트리에서 재현한다.

### 구현 태스크
- [x] 수정한 Markdown 7개의 로컬 링크를 전수 검사한다.
- [x] MCP-only guard와 browser storage 금지 조건을 검사한다.
- [x] 엔진 shard 생성 명령을 저장소 루트에서 실행한다.
- [x] 번들 budget과 엔진/MCP 교차검증을 재실행한다.
- [x] 이번 후속 조치에서 추가 소스 변경이 없는지와 whitespace 오류를 확인한다.

### 자체 테스트
- [x] `npm --prefix web-mcp-daily run guard:mcp-only:self-test`
- [x] `npm --prefix web-mcp-daily run guard:mcp-only`
- [x] `! rg "localStorage|sessionStorage" web-mcp-daily/src`
- [x] `npm --prefix engine run data-shards:generate -- --output ../web-lucky/public/manseryeok-data`
- [x] `node scripts/cross-check-engine-mcp.mjs`
- [x] `node scripts/web-demo-size-report.mjs --fail-on-budget ...`
- [x] `git diff --check`

### 이슈 및 수정
- [x] 추가 이슈 없음. Phase 2에서 발견한 잘못된 shard 출력 디렉터리는 제거 후 올바른 경로로 재생성했다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능


## Phase 4. 종료 상태 기록

### 목표
- 프로젝트를 종료할 때 보존되는 로컬 상태와 남은 외부·Git 작업을 명확히 기록한다.

### 구현 태스크
- [x] 로컬 구현·검증 완료 여부를 기록한다.
- [x] 실제 PlayMCP 등록, 운영 HTTPS 배포, PWA 검토를 미완료 외부 과제로 기록한다.
- [x] commit/push 미실행과 현재 변경 파일 수를 종료 시점 Git 상태로 기록한다.
- [x] 실행한 검증과 결과를 최종 결과 요약에 기록한다.

### 자체 테스트
- [x] `git status --short --branch`
- [x] `git rev-list --left-right --count HEAD...@{upstream}`
- [x] 실행 중인 검증용 서버가 남지 않았는지 확인한다.

### 이슈 및 수정
- [x] 후속 리뷰에서 shard 출력 경로 삭제 범위, 비동기 stale 응답, 교차검증 정책 누락, CI 인증 경로 누락을 확인해 수정했다.
- [x] Git 반영은 제외 범위에 따라 보류 상태다.

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 프로젝트 종료 상태가 과장 없이 기록됨

## 종료 판정 기준

- 로컬 구현 완료: 엔진/MCP/두 웹앱의 기존 기능 게이트가 통과한다.
- 문서 정합성 완료: README와 계획서가 같은 현재 상태, 명령, 외부 과제를 설명한다.
- Git 반영 완료: commit/push 후 `HEAD`, upstream, 작업트리가 일치할 때만 사용한다.
- 이번 작업의 완료는 `로컬 구현 완료 + 문서 정합성 완료`까지이며, Git 반영 완료는 사용자 요청 전까지 보류한다.

## 최종 결과 요약

후속 검증 완료: `2026-07-13 16:40 KST`

- Engine: type-check/build 통과, 테스트 224개 통과·3개 제외.
- MCP server: 테스트 108개 통과, 인증·dashboard-disabled production smoke 통과, PlayMCP preflight 12/12 통과.
- MCP response: `tools/call` 20개 `OK`, `tools/list` 53.3KB `WATCH`, `LARGE` 0개.
- web-lucky: 테스트 18개, build, 허브·shard·stale-response browser smoke 통과.
- web-mcp-daily: MCP-only guard/self-test, build, 인증·서버 OFF·stale-tab browser smoke 통과.
- 교차검증: 윤달·야자시·진태양시를 포함해 engine direct와 MCP `structuredContent` 10/10 일치.
- 웹 budget: web-lucky 385.4KB raw/88.0KB gzip, web-mcp-daily 42.5KB raw/13.5KB gzip으로 8개 budget 통과.
- Production dependency audit: engine, MCP server, 두 웹앱 모두 취약점 0개.
- 문서: 수정한 Markdown 8개 로컬 링크 누락 0개, `git diff --check` 통과.
- 프로세스: 검증용 3100/4173/3198/3199/5198/5199 포트 listener 없음.

## 종료 시점 Git 상태

- 브랜치: `main`, upstream `lucky/main`과 ahead/behind `0/0`.
- 작업트리: 기존 구현과 후속 안전성 보강을 포함해 tracked 수정 35개, untracked 16개, 총 51개 파일 경로.
- commit/push: 이번 범위에서 실행하지 않았다. 따라서 원격 `main`에는 현재 로컬 완료 상태가 아직 반영되지 않았다.

## 미완료 외부 과제

- PlayMCP 콘솔의 실제 "정보 불러오기"와 대표 tool call 확인.
- 장기 운영용 HTTPS 호스팅, domain/DNS/TLS, secret rotation, process manager 구성.
- 실제 오프라인 설치 요구가 확정될 경우에만 PWA/service worker 재검토.
